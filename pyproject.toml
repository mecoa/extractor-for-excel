[project]
name = "extractor-for-excel"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "PySide6",
    "openpyxl",
    "pandas",
    "httpx",
]

[project.scripts]
extractor-for-excel = "main:main"

[tool.ruff]
line-length = 100
target-version = "py311"
